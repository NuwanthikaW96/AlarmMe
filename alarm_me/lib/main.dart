import 'package:flutter/material.dart';
import 'package:alarm_me/initial.dart';

void main() => runApp(MyApp());

// Changed the hintText in th theme
//All the interface must be stateful since we have to call the location check in every intereface
